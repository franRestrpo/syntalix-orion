# Archivo .env autogenerado para glpi
# No editar directamente si se esta gestionando con Syntalix-Orion

GLPI_DOMAIN={{ GLPI__DOMAIN | default('glpi.localhost') }}
GLPI_DB_HOST=mariadb
GLPI_DB_PORT=3306
GLPI_DB_NAME={{ MARIADB__MYSQL_DATABASE | default('appdb', true) }}
GLPI_DB_USER={{ MARIADB__MYSQL_USER | default('orion_admin', true) }}
GLPI_DB_PASSWORD={{ MARIADB__MYSQL_PASSWORD | default('securepass', true) }}
GLPI_CRONTAB_ENABLED=1

