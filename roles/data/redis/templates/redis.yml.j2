version: '3.8'
services:
  redis:
    image: redis:7-alpine
    command: redis-server --requirepass {{ REDIS__REDIS_PASSWORD | default('secret', true) }}
    deploy:
      restart_policy:
        condition: on-failure
    env_file:
      - {{ playbook_dir }}/secrets/redis.env
    networks:
      SyntalixNet:
        aliases:
          - redis-cache
    volumes:
      - redis_data:/data

networks:
  SyntalixNet:
    external: true

volumes:
  redis_data:
